!dir
# This repo is connected to huggingface. I want to detach it from there and connect it to https://github.com/danshapiro/the-idiot-test which is a private repo. Be conservative; look what's at a location before doing something with it.
git remote -v
git remote remove origin
git remote add origin https://github.com/danshapiro/the-idiot-test.git
git remote -v
git push -u origin main
git pull origin main --rebase
dir #Wait, the files that were in this directory disappeared! What happened? 
# Got it. Just use the remote readme.
# You do it, I don't want to do it manually.
git checkout --theirs README.md && git add README.md && git rebase --continue
!dir
# It was waiting on an editor to close the file. It's done now.
!dir
git push -u origin main
